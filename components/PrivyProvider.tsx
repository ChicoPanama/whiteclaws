// Deprecated: Use app/Providers.tsx instead (already wraps the root layout).
// This component is not imported anywhere.
export default function PrivyProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
