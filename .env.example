# ─── Supabase ───
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ─── Privy (wallet auth) ───
NEXT_PUBLIC_PRIVY_APP_ID=
PRIVY_APP_ID=
PRIVY_APP_SECRET=

# ─── Encryption (finding submissions) ───
# Base64-encoded NaCl public key for encrypting submissions
# Generate with: node -e "const nacl=require('tweetnacl');const k=nacl.box.keyPair();console.log(Buffer.from(k.publicKey).toString('base64'))"
NEXT_PUBLIC_SUBMISSION_PUBLIC_KEY=

# ─── Optional ───
NEXT_PUBLIC_BUILD_STAMP=

# ─── Notifications (Resend) ───
RESEND_API_KEY=
FROM_EMAIL=findings@whiteclaws.xyz
NEXT_PUBLIC_APP_URL=https://whiteclaws.xyz
